package sec2ex;

public class Item implements Salable, ItemComp {
	String name;
	int price;
	
	public Item(String name, int price) {
		this.name = name;
		this.price = price;
	}
	
	@Override
	public String getName() {
		return this.name;
	}
	
	@Override
	public int getPrice() {
		return this.price;
	}
	
	@Override
	public int compareTo(Salable other) {
		int maxNum = 0;
		if (this.price > maxNum) {
			maxNum = this.price;
		} else if(this.price == maxNum) {
			this.price = 0;		
		}
		return this.price;
		
	}
}
